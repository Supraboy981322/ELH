<!DOCTYPE html>
<html lang="en">
    <head>
        <title>ELH</title>
        <link rel="stylesheet" href="/style.css">
    </head>
    <body>
        <h1>ELH</h1>
        <h3>Examples:</h3>
        <h3>Docs:</h3>
        <ul>
            <$bash
                dir="docs"
                files="$(ls ${dir})"
                for file in ${files}; do
                    name="${file}"
                    if [[ "${file}" == "index.elh" ]]; then
                        name="all"
                    fi
                    echo "<li><a href=\"${dir}/${file}\">${name}</a></li>"
                done
            $>
        </ul>
        <h3>Progress</h3>
        <div class="progTrack">
            <$bash
                readme="$(curl 'https://raw.githubusercontent.com/Supraboy981322/ELH/master/README.md')"
                todoString="### TODO"
                section="${readme#*${todoString}}"
                printf "${section}" | marked
            $>
        </div>
    </body>
</html>
