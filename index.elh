<!DOCTYPE html>
<html lang="en">
    <head>
        <title>egp</title>
        <link rel="stylesheet" href="/style.css">
    </head>
    <body>
        <h1><$js console.log("ELH"); $></h1>
        <h3>Examples:</h3>
        <h3>Docs:</h3>
        <ul>
            <$bash
                dir="docs"
                files="$(ls ${dir})"
                for file in ${files}; do
                    name="${file}"
                    if [[ "${file}" == "index.elh" ]]; then
                        name="all"
                    fi
                    echo "<li><a href=\"${dir}/${file}\">${name}</a></li>"
                done
            $>
        </ul>
    </body>
</html>
